# unit tests.

---

This directory has python files to test the individual functions. e.g to teas the noop speeds run python3 or python with nop.py to run tests. this is just an example as there are currently no unit tests 